// Addresses of the site that the core writes in its messages, in a module of their own: the modules that need
// them import nothing else through it.

// The page of the site that checks the services the maps depend on.
export const STATUS_PAGE_URL = 'https://sebprunier.github.io/cartes/etat-des-services.html';
